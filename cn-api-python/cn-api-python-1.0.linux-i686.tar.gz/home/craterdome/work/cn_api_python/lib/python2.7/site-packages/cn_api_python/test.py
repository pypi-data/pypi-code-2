import unittest

from api import CharityAPI

class TestApiCalls(unittest.TestCase):
    def setUp(self):
        self.api = CharityAPI()

    def test_categories(self):
        pass
#        new = self.api.create_category({
#            'category': 'Test Category',
#            'description': 'This is just a test'
#        })
#        new_category = self.api.get_category(new['categoryid'])
#        self.assertEqual(new_category['description'], 'This is just a test')
#        
#        self.api.update_category(new['categoryid'], {'description': 'Updated'})
#        updated_category = self.api.get_category(new['categoryid'])
#        self.assertEqual(updated_category['description'], 'Updated')
#        
#        category_ids = [cat['categoryid'] for cat in self.api.list_categories()['objects']]
#        self.assertIn(new['categoryid'], category_ids)
        
if __name__ == '__main__':
    unittest.main()